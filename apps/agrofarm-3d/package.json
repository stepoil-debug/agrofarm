{
  "name": "@agrofarm/game-3d",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "premium-fix": "node scripts/apply-premium-fix.mjs",
    "predev": "npm run premium-fix",
    "dev": "vite --host 0.0.0.0",
    "precheck": "npm run premium-fix",
    "check": "tsc --noEmit",
    "prebuild": "npm run premium-fix",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.1.5"
  }
}
